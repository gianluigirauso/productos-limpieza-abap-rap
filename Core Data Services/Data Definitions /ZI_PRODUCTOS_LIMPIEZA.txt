@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Productos de Limpieza Raiz'
define root view entity ZI_PRODUCTOS_LIMPIEZA
  as select from zlimpieza_prod
{
  key prod_id as ProdId,
  nombre as Nombre,
  marca as Marca,
  @Semantics.amount.currencyCode: 'Moneda'
  precio as Precio,
  moneda as Moneda,
  stock as Stock,
  local_last_changed_at as LocalLastChangedAt,
  last_changed_at as LastChangedAt
}
